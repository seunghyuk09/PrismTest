# PrismTest — 투명 프리즘 표면 얼룩 검사

## 프로젝트 목표
스마트폰 카메라로 **투명 프리즘**을 촬영해 실시간으로 양/불을 판정하는
**모바일 앱(iOS + Android)** + 조명 지그를 만든다. 최우선 타깃 결함은 **표면 얼룩**이다.

- 폰을 지그에 고정 → 프리즘을 지그 안착부에 올림 → 앱 카메라 화면에서 즉시 PASS/FAIL
- 판정은 사람 눈이 아니라 **수치(결함 지수 + 임계값)** 로 낸다
- 애매하면 판정을 강행하지 말고 "재촬영/재안착"을 띄운다

### 지원 기기 정책
1차 검증 기기는 **갤럭시 Z 폴드 7**(보유 기기), 이후 **iPhone 포함 타 기종으로 확대**한다.
단, "아무 폰이나 설치하면 검사됨"은 성립하지 않는다 — 기기마다 센서·렌즈·ISP가 달라
같은 프리즘도 다른 지수가 나온다. 따라서:

- 알고리즘은 **C++ 공용 코어 하나**로 짜고 iOS·Android가 동일하게 호출한다 (숫자가 갈라지면 끝)
- 기기마다 **캘리브레이션 프로파일**(노출·초점·px/mm·플랫필드·σ0·임계값)을 별도로 만든다
- 프로파일이 없는 기기는 앱이 **판정 불가로 잠근다**
- 신규 기기는 `prism-calibration`의 **등가성 검증(Kappa ≥ 0.8, R² ≥ 0.9)** 을 통과해야 등록
- iOS는 노이즈리덕션·샤프닝을 끌 수 없고 사이드로드도 불가 → 세부 사항은 `docs/platform-strategy.md`

## 대상물 (도면 확정 → `docs/prism-spec.md`)
- **U20 PRISM** (SecuGen, SG_PG_20&UPx_V1.2). **이등변** 삼각 프리즘 — 직각 아님
- 꼭지각 **42.0°±0.1°**, 밑각 **69.0°±0.1°**, S1=S2=**24.5 mm**, S3=17.56 mm, 깊이 **24.0 mm**
- **검사면 = S1, S3 두 면 전체**(CLEAR APERTURE). S2는 연마·페인트면이라 대상 아님
- 스펙 **Scratch-Dig 60/40** → **Dig 40 = 0.40 mm**. 1024 px ROI에서 42 px/mm, 0.4 mm = 17 px
  → **폰 카메라 해상도는 병목이 아니다.** 병목은 조명과 재현성
- ⚠️ **재질 불일치 미해결**: 도면은 H-F4 **광학유리**(n=1.6200), 검사기준서·현장은 **플라스틱**(U20-AP)
  → 굴절률이 임계각을 바꾸고 곧 관찰 각도를 바꾼다. 실제 검사 대상 확정 필요
- 검사 단계에서는 **완전 투명**
- 페인트는 WK 공정에서 **조립 직전에 도포** (참고 사진이 흑색인 이유)
- **닦을 수 없다** — 플라스틱이라 닦으면 스크래치가 생긴다. 재세정 검증 불가
- 불량은 **폐기 확정**(재작업 경로 없음) → **과검 1건 = 폐기 1건**
- 실치수·재질 상세(PMMA/PC)·검출 최소 결함 크기는 **미확정 → 확인 필요**

## 현행 검사 (사내 자료 분석 → `docs/current-inspection.md`)
- 지그 = **Ulanzi VL49 LED 패널(CRI 95+) + 삼각대**, 프리즘은 **손으로 들고** 비스듬히 관찰, 밝기 최대
- **광학 원리는 이미 옳다**(측면 그레이징 = 사실상 암시야). 조명을 새로 발명할 필요 없음
- 결정적 결함 두 가지: **배경이 흰색**(암시야 미완성), **손으로 듦**(재현성 없음)
- → 앱 지그는 현행에 **검은 배경 + 안착부 + 폰 거치대** 세 개만 추가한다
- 배경은 평면 검정이 아니라 **광트랩 구조**로 만든다 (재료보다 구조가 이긴다).
  검은 벨벳/기모 + 10 cm 이격 + 차광판. 검증은 배경 평균 밝기 ≤ 8/255 → `docs/dark-background.md`
- **"표면 불량"은 얼룩이 아니라 성형 불량**(플로우마크/실버스트릭/가스 자국). 그래서 안 닦인다
- "동일위치" 결함(찍힘·스크래치) = 금형 유래 → ROI를 좁혀 집중 검사 가능
- 버(Burr)는 촉각 검사 → **카메라 대체 불가, 앱 범위 제외**
- OPL·SG·WK **3중 육안검사** → 앱 효과 큼. 단 세 공정의 기준 일치 여부 확인 필요

## 결함의 정체 — 사출 시 갇힌 가스 (`docs/gas-defect.md`)
사출 시 수지 내 가스가 **보압 단계에서 밀리며 갇혀** 표면 또는 내부에 생긴다.
**「표면 얼룩」과 「내부 가스/백태」는 원인이 같고 위치만 다르다.** 분류 오류가 아니다.

- **위치에 따라 사용 가능한 개체가 있는데 현재는 전부 폐기 중** → 과잉 폐기 가능성
- S3는 전반사면이라 **S3 표면의 가스는 치명적**, 광로 밖 내부 가스는 무해할 수 있다
- → 판정은 `f(크기, 대비, **위치**)`. 단일 임계값으로는 못 가른다
- → FTIR 암시야는 표면에만 민감, 투과광은 내부에 민감 → **2뷰로 위치가 분리된다**
- 앱의 가치가 "불량 선별"에서 **"과잉 폐기 감소"** 로 확장된다

## 누적 불량 이력 (2024-07~2026-01, n=61,488 → `docs/defect-history.md`)
| 항목 | 값 |
|---|---:|
| 총 입고 / 양품 / 불량 | 61,488 / 51,632 / **9,856** |
| 누적 불량률 | **16.03%** |

- 누적 파레토: 표면 얼룩 61.8% → **흰색 이물 23.5%**(2위) → 스크래치 7.4% → 검정 이물 5.2%
  **표면 얼룩 + 흰색 이물이 85.3%.** 앱은 이물도 함께 잡아야 한다
- **표면 얼룩 6,088건 중 5,843건(96%)이 마지막 한 로트에 집중** — 만성 불량이 아니라 사건
- 불량률: 37.6%→24.8%→2.4%→1.6%→**0.58%**→1.6%→6.9%→**44.9%**. 잡았다가 다시 무너진 공정
- 같은 날 2026-01-23 두 로트가 6.94% vs 44.92% → **합산 해석 금물**
- **앱의 실질 ROI = 조기 감지.** 1차(6.94%)에서 잡았다면 2차의 6,792개를 막았다. 3중 육안검사로 못 막음

## 불량 파레토 (2026-01-23 2차 로트 단독, n=15,120)
| 항목 | 수량 | 비율 |
|---|---|---|
| 양품 | 8,328 | 55.08% |
| **표면 얼룩** | **5,843** | **38.64%** |
| 흰색 이물 | 512 | 3.39% |
| 스크래치 | 244 | 1.61% |
| 검정색 이물 | 118 | 0.78% |
| 페인트 얼룩 | 75 | 0.50% |
| Diffuser 얼룩 / Dig / 파손 / 내부 기포 / 내부 백태 | 0 | 0.00% |

→ 이 로트에서는 표면 얼룩이 불량의 87.9%. **단 이것은 이 한 로트의 수치이며
누적 기준은 61.8%다** (위 「누적 불량 이력」 참조). 이 표만 보고 설계하면 안 된다.

기준서의 「내부 가스」가 이 표에 0건인 것은 분류 오류가 아니라 **같은 가스 결함이
위치에 따라 「표면 얼룩」으로 계상되기 때문**이다 → `docs/gas-defect.md`.
표면/내부 구분 수단은 **조명 2뷰(FTIR 암시야 vs 투과광)** 와 **시차(parallax) 테스트**다.
플라스틱이라 재세정 검증은 쓸 수 없다.

## 작업 분담 (서브에이전트)
| 에이전트 | 역할 |
|---|---|
| `prism-optics-jig` | 조명·광학 배치, 기기 교체식 지그 설계, 촬영 조건 고정, 기기 최소 요건 |
| `prism-defect-taxonomy` | 결함 정의·광학적 시그니처·데이터셋/라벨링 프로토콜 |
| `prism-vision-algo` | 영상처리 파이프라인, 결함 지수 산출 (Python 프로토타입 → C++ 코어 사양) |
| `prism-mobile-app` | iOS·Android 실시간 검사 앱 (공용 C++ 코어 + 네이티브 셸) |
| `prism-calibration` | 임계값 결정, 과검/미검, Gauge R&R, 기기 간 등가성 검증, 현장 검증 |

## 원칙
- **광학이 8할**: 알고리즘으로 억지로 짜내기 전에 조명으로 결함을 눈에 보이게 만든다.
- **모든 촬영 조건 고정**: ISO/셔터/포커스/WB/줌 수동 고정, HDR·씬 최적화·나이트모드 전부 OFF.
- **수치화 필수**: 모든 판정은 재현 가능한 스칼라 지수와 임계값으로 설명된다.
- **미검 > 과검 회피**: 초기 임계값은 과검 쪽으로 보수적으로 잡고 데이터로 완화한다.
