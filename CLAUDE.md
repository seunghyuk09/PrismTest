# PrismTest — 투명 프리즘 표면 얼룩 검사

## 프로젝트 목표
삼성 갤럭시 폴드 7 카메라로 **투명 프리즘**을 촬영해 실시간으로 양/불을 판정하는
안드로이드 앱 + 조명 지그를 만든다. 최우선 타깃 결함은 **표면 얼룩**이다.

- 폰을 지그에 고정 → 프리즘을 지그 안착부에 올림 → 앱 카메라 화면에서 즉시 PASS/FAIL
- 판정은 사람 눈이 아니라 **수치(결함 지수 + 임계값)** 로 낸다
- 애매하면 판정을 강행하지 말고 "재촬영/재안착"을 띄운다

## 대상물
- 직각 삼각 프리즘(우측 사진). 검사 시에는 **완전 투명**(사진은 흑색 페인트 도포 상태의 참고용)
- 실제 치수·재질(유리/아크릴)·유효 검사면·페인트 유무 조건은 **미확정 → 확인 필요**

## 불량 파레토 (2026-01-23, n=15,120)
| 항목 | 수량 | 비율 |
|---|---|---|
| 양품 | 8,328 | 55.08% |
| **표면 얼룩** | **5,843** | **38.64%** |
| 흰색 이물 | 512 | 3.39% |
| 스크래치 | 244 | 1.61% |
| 검정색 이물 | 118 | 0.78% |
| 페인트 얼룩 | 75 | 0.50% |
| Diffuser 얼룩 / Dig / 파손 / 내부 기포 / 내부 백태 | 0 | 0.00% |

→ 표면 얼룩 하나가 전체 불량의 **87.9%**. 여기만 잡아도 효과의 대부분을 가져간다.
→ 단, 얼룩과 혼동되기 쉬운 **내부 백태 / 흰색 이물**과의 분리는 알고리즘에서 반드시 다룬다.

## 작업 분담 (서브에이전트)
| 에이전트 | 역할 |
|---|---|
| `prism-optics-jig` | 조명·광학 배치, 지그 설계, 촬영 조건 고정 |
| `prism-defect-taxonomy` | 결함 정의·광학적 시그니처·데이터셋/라벨링 프로토콜 |
| `prism-vision-algo` | 영상처리 파이프라인, 결함 지수 산출 (Python 프로토타입) |
| `prism-android-app` | CameraX/OpenCV 실시간 검사 앱 (Kotlin, APK) |
| `prism-calibration` | 임계값 결정, 과검/미검, Gauge R&R, 현장 검증 |

## 원칙
- **광학이 8할**: 알고리즘으로 억지로 짜내기 전에 조명으로 결함을 눈에 보이게 만든다.
- **모든 촬영 조건 고정**: ISO/셔터/포커스/WB/줌 수동 고정, HDR·씬 최적화·나이트모드 전부 OFF.
- **수치화 필수**: 모든 판정은 재현 가능한 스칼라 지수와 임계값으로 설명된다.
- **미검 > 과검 회피**: 초기 임계값은 과검 쪽으로 보수적으로 잡고 데이터로 완화한다.
